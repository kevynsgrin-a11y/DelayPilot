{
  "name": "@delaypilot/contracts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared Zod schemas and inferred types — the single source of truth for every shape that crosses a package, route, or component boundary.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "eslint ."
  }
}

packages:
  - 'apps/*'
  - 'packages/*'

# pnpm 10 does not run dependency lifecycle scripts unless the package is listed here.
# Each entry below ships a platform binary that the toolchain cannot work without.
onlyBuiltDependencies:
  - esbuild
  - workerd
